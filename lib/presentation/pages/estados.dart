import 'package:flutter/material.dart';

class Pageestados extends StatelessWidget {
  const Pageestados({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Estados", style: TextStyle(fontSize: 30),)
    );
  }
}